// install all the needed packages
= pip install numpy pandas matplotlib statsmodels pandas_datareader datetime yfinance scikit-learn PyPortfolioOpt
// configured the needed packages by = pip freeze > requirements.txt
// install the needed packages with requirements.txt = pip install -r requirements.txt
 first = download the s&p 500 companies
 second = calculate the features and the indicators 
 garman - klass
 rsi
 bollinger bands
 arp
 macd